pnpm run build
pnpm run check
pnpm run test
if ! git diff --quiet || ! git diff --cached --quiet; then
  echo "Working directory is not clean — skipping biz42 validate"
else
  pnpm run --silent validate:source
fi
