# Architecture Document

The architecture of biz42 mirrors arc42-language exactly.
The authoritative architecture documentation lives in `docs/arc42/` — copied from
arc42-language and adapted to the biz42 domain.

**→ See `docs/arc42/`**

All structural decisions (monorepo layout, package boundaries, processing pipeline,
DSL format, CLI pattern, SPA, rule registry) carry over 1:1 — adapted to the
biz42 domain model (12 ISO 9001 sections instead of 13 arc42 chapters).
